# -*- coding: utf-8 -*-
"""SimplyBook.me integration cluster - one quote-based pillar + five firsthand guides.

WHY THIS EXISTS (researched 2026-07-26): demand for paid SimplyBook integration help is
a trickle, but the niche is COMPLETELY unclaimed - no firm anywhere positions as a
SimplyBook integration specialist, and the technical content on the open web amounts to
a couple of official examples. Several things documented here (notably the undocumented
client_fields parameter surfaced by error -32070) appear nowhere else, which is what
makes these pages both rankable and citable by AI assistants.

HONESTY RULES BAKED IN - do not regress:
  * 365 Techies is NOT a SimplyBook partner, reseller or affiliate. Say so plainly.
  * No prices anywhere. The service is quoted per project (the VRM dashboards pattern).
  * No SLAs or response-time promises - a two-person firm cannot keep them on a
    platform it does not control.
  * Every technical claim here was verified on 365's own live integration in July 2026.
This is a SMALL BET with kill criteria: if 60-90 days brings no qualified enquiries and
negligible impressions, stop writing more - leave these live, they cost nothing.
"""
from build_extra import info_page

_SB_DIRECTORY = '\n<section class="section section--alt" aria-label="SimplyBook guides">\n  <div class="wrap">\n    <div class="section-head">\n      <p class="eyebrow eyebrow--center mono" data-reveal>// WHAT WE LEARNED, WRITTEN DOWN</p>\n      <h2 class="section-title section-title--center" data-title>SimplyBook technical guides<span class="title-underline title-underline--center"></span></h2>\n      <p class="lede lede--center" data-reveal>Everything below came out of building our own integration &mdash; including several things SimplyBook&rsquo;s documentation gets wrong. Free, no sign-up, no email required.</p>\n    </div>\n    <div class="tile-grid" data-stagger>\n      <a class="tile tile--link" href="/simplybook-addclient-value-is-required-error/" data-reveal><h3>&ldquo;Value is required and can&rsquo;t be empty&rdquo;</h3><p>The addClient error that names no field &mdash; and the undocumented parameter that fixes it.</p></a>\n      <a class="tile tile--link" href="/simplybook-callback-url-not-working/" data-reveal><h3>Callback URL not working</h3><p>What SimplyBook actually posts, how to secure an unsigned webhook, and why silence is the real danger.</p></a>\n      <a class="tile tile--link" href="/simplybook-custom-booking-page/" data-reveal><h3>Your own booking page</h3><p>Replacing the widget without ever exposing an API key to the browser.</p></a>\n      <a class="tile tile--link" href="/simplybook-custom-email-notifications/" data-reveal><h3>Taking over the emails</h3><p>Own your confirmations and your review ask &mdash; and the switch-off order that avoids missed appointments.</p></a>\n      <a class="tile tile--link" href="/simplybook-integration-traps/" data-reveal><h3>Six production traps</h3><p>One booking that became twelve, invites an hour out, and four more that cost us real time.</p></a>\n    </div>\n  </div>\n</section>\n'

info_page(
    slug='simplybook-integration',
    crumb_name='SimplyBook Integration',
    title='Custom SimplyBook.me Integration | Your Booking, Your Brand | 365 Techies',
    og_title='Custom SimplyBook.me Integration',
    eyebrow='// SIMPLYBOOK INTEGRATION',
    h1='Custom <em class="grad grad--cyan">SimplyBook.me</em> integration',
    lede='Keep SimplyBook as the engine and put your own brand in front of it &mdash; your booking screens, your emails, your customer area. We built exactly this for our own business, and you can book through it right now.',
    desc='Custom SimplyBook.me integration: your own booking page, your own emails and your own customer area, with SimplyBook working invisibly behind them. Built and proven on our own site. Quote-based, from a UK team.',
    chips=['Your brand, not theirs', 'We built our own', 'Quoted per project'],
    hero_cta1=('See our live booking page', '/book-service/'),
    hero_cta2=('Talk it over', '/contact/'),
    pre=_SB_DIRECTORY,
    inner='<h2>What a custom SimplyBook integration actually means</h2>\n<p>Most SimplyBook customers use it the way it ships: a widget dropped onto a page, or a link out to a <code>*.simplybook.it</code> address. It works. It also means the most important moment in your customer journey &mdash; the point where someone decides to give you money &mdash; happens inside somebody else&rsquo;s interface, followed by somebody else&rsquo;s emails.</p>\n<p>A custom integration flips that round. SimplyBook stays as the engine; everything the customer touches is yours:</p>\n<ul>\n<li><strong>Your own booking screens</strong>, built into your site with your own fonts, colours, wording and layout &mdash; no iframe, no third-party widget, no jump to another domain.</li>\n<li><strong>Your own emails</strong> &mdash; confirmation, reminders, changes, the follow-up review ask &mdash; sent from your domain, in your voice.</li>\n<li><strong>Your own customer area</strong>, where people sign in to see and manage their bookings alongside whatever else you offer them.</li>\n<li><strong>SimplyBook invisible</strong> behind all of it, still doing the scheduling, still the thing your staff open in the morning.</li>\n</ul>\n<p>Ours is live and bookable right now at <a href="/book-service/">/book-service/</a>. Nothing on that page is a SimplyBook widget. Every service, every free slot and every confirmation on it comes through our own code.</p>\n\n<h2>Why you would bother</h2>\n<p><strong>Brand consistency.</strong> A widget that doesn&rsquo;t quite match your site reads as &ldquo;this bit was outsourced&rdquo;. Customers rarely say it out loud; they just trust the page slightly less.</p>\n<p><strong>Control of the journey.</strong> With your own UI you decide the order of the questions, what&rsquo;s optional, what gets explained, where you offer reassurance, and what happens after the booking. Widgets give you a settings screen; code gives you the journey.</p>\n<p><strong>Owning the emails.</strong> This is the one owners underestimate. The confirmation and reminder emails are the most-opened messages your business sends. Owning them means you write them, you time them, you decide the quiet hours, and the review request goes to your Google profile in your words at the moment you choose.</p>\n<p><strong>No third-party iframe.</strong> Fewer moving parts on the page, no cross-domain quirks, and your booking flow keeps working the way you built it rather than changing shape when a vendor redesigns their widget.</p>\n\n<h2>What stays SimplyBook&rsquo;s job &mdash; and why that&rsquo;s the right call</h2>\n<p>The temptation, once you&rsquo;re writing code, is to write the whole thing. Don&rsquo;t. A booking engine is much harder than it looks, and the parts that are hard are exactly the parts SimplyBook already does properly:</p>\n<ul>\n<li><strong>Availability rules</strong> &mdash; working hours, breaks, per-service durations, buffers, holidays, seasonal changes.</li>\n<li><strong>Double-booking prevention</strong> &mdash; the race condition where two people click the same 10am. Getting this right under concurrency is a genuine engineering problem.</li>\n<li><strong>The staff diary</strong> &mdash; a mature admin interface your team already knows, on their phones, that you did not have to build or support.</li>\n<li><strong>An off-site copy of the diary.</strong> If your website has a bad day, the appointments still exist somewhere else and staff can still see them. That is worth real money.</li>\n</ul>\n<p>So the honest architecture is: buy the hard scheduling logic, build the experience. Ours is a small PHP proxy on ordinary shared hosting. All credentials live server-side; the browser only ever talks to our proxy, never to SimplyBook. Public read-only calls (service list, availability) are rate-limited per IP and cached for about 90 seconds; anything that writes requires proof of identity first.</p>\n\n<h2>The honest limits</h2>\n<p>Some things cannot be moved behind your brand, and you should know before you start:</p>\n<ul>\n<li><strong>SimplyBook&rsquo;s own notification templates are UI-only.</strong> The API cannot edit them. If you want your own emails you don&rsquo;t customise theirs &mdash; you switch the relevant ones off in the admin and send your own. That means you also inherit the boring parts: queueing, retries, deduplication and email deliverability.</li>\n<li><strong>The documentation is incomplete in places.</strong> We hit a create-client call that failed forever with a generic &ldquo;value is required&rdquo; message because the documented field list left out required custom client fields. The live JSON-RPC error payload names the missing field exactly &mdash; most client libraries just truncate it and hide the answer.</li>\n<li><strong>Platform changes are outside your control.</strong> It is someone else&rsquo;s product. Settings screens get renamed, defaults shift. Build so that a failure is loud rather than silent, and re-test after any account change.</li>\n<li><strong>Some defaults are quietly dangerous.</strong> The callback URL is empty by default, so bookings made inside SimplyBook reach your server not at all, with no warning anywhere. Recurring services return the whole series from one booking call, so reading only the first entry hides eleven other appointments. Both cost us time before we caught them.</li>\n</ul>\n\n<h2>What a project involves</h2>\n<ol>\n<li><strong>Look at what you have.</strong> Your services, staff, rules, current emails, and what the customer sees today. Half the value is often in decisions, not code.</li>\n<li><strong>Prove the connection.</strong> A server-side proxy, tokens cached properly, read-only endpoints first: services and real availability rendered on your own page.</li>\n<li><strong>Booking, then identity.</strong> Creating clients and bookings reliably &mdash; including the custom-field traps &mdash; then a sign-in method that actually proves who someone is. A SimplyBook client login only proves someone controls that booking account; it is not proof of being your customer. We use our own emailed-code sign-in.</li>\n<li><strong>Take over the emails.</strong> Turn off the ones you&rsquo;re replacing, build a queue with retries and quiet hours, and get authentication right so they land in inboxes rather than spam.</li>\n<li><strong>Make silence visible.</strong> Webhooks, monitoring, and an alert when nothing has arrived for a while &mdash; &ldquo;configured&rdquo; is not the same as &ldquo;working&rdquo;, especially with hosting bot-protection in front of your endpoint.</li>\n<li><strong>Hand over.</strong> Plain notes on which settings live where, so you&rsquo;re not dependent on remembering.</li>\n</ol>\n\n<h2>How we work on this</h2>\n<p>Pricing is per project and quoted after we&rsquo;ve had a proper look &mdash; the work varies enormously depending on how many services and rules you have and how much of the email stack you want to own. We are not affiliated with, endorsed by or a partner of SimplyBook.me; we are simply customers of theirs who built an integration and learned where the edges are.</p>\n<p>We built this for our own booking system in 2026, because we wanted our customers to stay on our site and get our emails. It is running, it is taking real bookings, and everything on these pages is what we found doing it. If you&rsquo;d like the same for yours, we&rsquo;re happy to talk it through.</p>',
    faqs=[
        ('Do we have to leave SimplyBook?', 'No &mdash; and usually you shouldn&rsquo;t. SimplyBook stays as the diary engine doing the genuinely hard parts: availability rules, double-booking prevention, the staff calendar your team already knows, and an off-site copy of your bookings. We change what your <em>customers</em> see, not what your staff use.'),
        ('Can you show us something real?', 'Yes &mdash; our own booking page at <a href="/book-service/">365techies.co.uk/book-service/</a> is a custom SimplyBook integration. Nothing on it is a SimplyBook widget: the services, the live availability and the confirmation all come through our own code.'),
        ('What does it cost?', 'It&rsquo;s quoted per project after we&rsquo;ve had a proper look, because the work varies enormously with how many services and rules you have and how much of the email side you want to own. We don&rsquo;t publish a price we&rsquo;d have to walk back.'),
        ('Are you a SimplyBook partner or reseller?', 'No. We&rsquo;re not affiliated with, endorsed by or a partner of SimplyBook.me &mdash; we&rsquo;re customers of theirs who built a deep integration for our own business and write about what we learned.'),
        ('Can you do this if our site isn&rsquo;t built by you?', 'Often yes, though it depends what your site runs on. The integration lives in a small server-side component plus the booking screens themselves, so the honest answer needs a look at your setup first.'),
    ],
    cta_args=('Thinking about your own booking experience?', 'We built ours on SimplyBook and it is live on this site &mdash; have a look, then tell us what you are trying to do. Quoted per project, honestly, after a proper look.', ('See our live booking page', '/book-service/'), ('Talk it over', '/contact/')),
)

info_page(
    slug='simplybook-addclient-value-is-required-error',
    crumb_name='addClient &ldquo;value is required&rdquo;',
    title='SimplyBook addClient: &ldquo;Value is required and can&rsquo;t be empty&rdquo; &mdash; Solved',
    og_title='SimplyBook addClient &ldquo;Value is required&rdquo; &mdash; solved',
    eyebrow='// SIMPLYBOOK API',
    h1='SimplyBook <em class="grad grad--cyan">addClient</em> fails: &ldquo;Value is required and can&rsquo;t be empty&rdquo;',
    lede='The error names no field, the documentation says the cause is impossible, and every obvious fix misses. Here is what is actually happening &mdash; and the undocumented parameter that fixes it.',
    desc='SimplyBook.me addClient failing with "Value is required and can\'t be empty"? The -32070 error payload names the missing custom client field, and an undocumented client_fields parameter fixes it. Full worked solution from a live integration.',
    chips=['Error -32070', 'Undocumented fix', 'Verified live'],
    hero_cta1=('Custom SimplyBook integration', '/simplybook-integration/'),
    hero_cta2=('Talk to us', '/contact/'),
    pre=None,
    inner='<h2>The symptom</h2>\n<p>You have a working SimplyBook admin token. Your JSON-RPC calls to <code>https://user-api.simplybook.me/admin/</code> are authenticating cleanly. Then you call <code>addClient</code> with a perfectly sensible client object &mdash; name, email, phone &mdash; and get back this:</p>\n<pre><code>Value is required and can\'t be empty</code></pre>\n<p>No field name. No hint. The same message appears whatever you send. You add an address. Same error. You add a country id. Same error. You strip it back to just a name. Same error. We spent an afternoon on this while building our own booking system in July 2026, and the frustrating part is that the API already knows exactly which field is missing &mdash; it just doesn&rsquo;t put it in the message.</p>\n\n<h2>Why the message is so unhelpful</h2>\n<p>The string you see is the top-level <code>error.message</code> of the JSON-RPC envelope. It is a generic validation message, reused for every required-field failure across the API. Most HTTP clients, most logging setups and most thin JSON-RPC wrappers surface <code>error.message</code> and stop there. So the one useful piece of information &mdash; which field &mdash; never reaches your eyes. The bug isn&rsquo;t really in SimplyBook; it&rsquo;s in the layer between you and it.</p>\n\n<h2>The false trails</h2>\n<h3>getCompanyParam(\'require_fields\')</h3>\n<p>This looks like the answer and isn&rsquo;t. It returns which <strong>built-in</strong> fields your company requires. On our account it returned <code>email</code> &mdash; which we were already sending. It tells you nothing about custom client fields, so a clean pass here can leave you convinced the problem must be elsewhere.</p>\n<h3>Mandatory registration fields</h3>\n<p>In SimplyBook admin, Settings &rarr; Email and SMS settings has a &ldquo;Mandatory registration fields&rdquo; option. That genuinely can block API client creation, so it is worth checking. But it wasn&rsquo;t our cause, and turning it off didn&rsquo;t change the error.</p>\n<h3>The documentation saying custom fields have no API</h3>\n<p>This is the trail that costs you the most time. SimplyBook&rsquo;s documentation for the company administration service lists <code>addClient</code> as accepting <em>only</em> <code>name</code>, <code>email</code>, <code>phone</code>, <code>address1</code>, <code>address2</code>, <code>city</code>, <code>zip</code> and <code>country_id</code>, and states that custom Client Fields have no API surface. Read literally, that means a required custom field makes API client creation impossible and your only option is to disable the field in admin. That is wrong. The live API accepts custom fields perfectly well.</p>\n\n<h2>The breakthrough: error code &minus;32070</h2>\n<p>Print the <em>whole</em> error object rather than the message, and the picture changes. The failure comes back as JSON-RPC error code <code>-32070</code>, and its <code>data</code> member carries a <code>field</code> key naming the exact culprit:</p>\n<pre><code>{\n  "error": {\n    "code": -32070,\n    "message": "Value is required and can\'t be empty",\n    "data": { "field": "client_fields/9f7e5581a1b24c0e8d3f6b27c4e01a5d" }\n  }\n}</code></pre>\n<p>That 32-character hex string is the internal id of a custom Client Field on your account (Custom Features &rarr; Client Fields; on newer accounts the screen is called &ldquo;Intake form for clients&rdquo;). The API has been telling you the answer the whole time.</p>\n\n<h2>The shape that works</h2>\n<p>Supply a <code>client_fields</code> map, keyed by those ids, inside the client-data object you pass to <code>addClient</code>. Despite the documentation, it is accepted:</p>\n<pre><code>{\n  "jsonrpc": "2.0",\n  "id": 1,\n  "method": "addClient",\n  "params": [\n    {\n      "name":  "Jane Smith",\n      "email": "jane@example.com",\n      "phone": "07700 900123",\n      "client_fields": {\n        "9f7e5581a1b24c0e8d3f6b27c4e01a5d": "Bournemouth"\n      }\n    }\n  ]\n}</code></pre>\n<p>We verified this live with a dry-run create that auto-filled one required custom field: the call that had failed indefinitely succeeded on the first attempt once the map was present.</p>\n\n<h2>The reusable technique: attempt, read, fill, retry</h2>\n<p>Because you can&rsquo;t enumerate the fields from the docs, treat the error as a discovery channel. The loop is small and it is the part worth stealing:</p>\n<ol>\n<li>Call <code>addClient</code> with what you have.</li>\n<li>If it succeeds, you&rsquo;re done.</li>\n<li>If the error code is <code>-32070</code> and <code>data.field</code> matches <code>client_fields/&lt;id&gt;</code>, extract the id.</li>\n<li>Add that id to your <code>client_fields</code> map with a sensible value &mdash; a real answer if you have one, otherwise a neutral placeholder your staff will recognise.</li>\n<li>Retry. Repeat for each newly named field, with a hard cap (we use five) so a field that refuses every value can&rsquo;t spin forever.</li>\n<li>Log the ids you discovered, along with any value you had to invent, so a human can review them later.</li>\n</ol>\n<pre><code>$fields = [];\nfor ($attempt = 0; $attempt &lt; 5; $attempt++) {\n    $res = $api-&gt;addClient($client + [\'client_fields\' =&gt; $fields]);\n    if (!isset($res[\'error\'])) { return $res; }\n\n    $err = $res[\'error\'];\n    if (($err[\'code\'] ?? null) !== -32070) { throw new Exception($err[\'message\']); }\n\n    $named = $err[\'data\'][\'field\'] ?? \'\';\n    if (strpos($named, \'client_fields/\') !== 0) { throw new Exception($err[\'message\']); }\n\n    $id = substr($named, strlen(\'client_fields/\'));\n    if (isset($fields[$id])) { throw new Exception(\'Field \' . $id . \' rejected our value\'); }\n    $fields[$id] = $this-&gt;valueFor($id);   // real answer, else a recognisable placeholder\n}</code></pre>\n<p>Fill one field per round rather than guessing several at once. The API names them one at a time, and a loop that respects that stays honest about what it actually learned.</p>\n\n<h2>Never truncate an error payload</h2>\n<p>The reason this knowledge is so hard to find is mundane: <code>error.data</code> is routinely thrown away. Client libraries map JSON-RPC faults onto exceptions carrying only code and message. Log formatters cap strings at 200 characters. Error trackers store the message and drop the structured extras. Any one of those is enough to hide the field name forever. When you integrate with an unfamiliar JSON-RPC API, log the entire error object verbatim before anything else touches it &mdash; and if your wrapper doesn&rsquo;t expose <code>data</code>, replace the wrapper.</p>\n\n<h2>The ids are per-account, so discover them at runtime</h2>\n<p>Those 32-hex ids belong to your company&rsquo;s configuration, not to SimplyBook globally. Hard-coding the one you found in development will break the moment you point at a different account &mdash; a client&rsquo;s, a staging company, or your own after someone edits the intake form. Keep the discovery loop in production, cache what it finds per account, and let it re-learn if a new field appears. That way an admin adding a required question to the intake form is a logged event rather than a silent outage.</p>\n\n<p>We worked this out building our own booking system, where SimplyBook.me is only the diary engine behind a PHP proxy and customers never see it. If you are wrestling with the same integration, or want something similar built properly for your business, 365 Techies can help.</p>',
    faqs=[
        ('Why does the error not say which field?', 'It does &mdash; but not in the message. The JSON-RPC error object carries <code>data.field</code> naming the exact field as <code>client_fields/&lt;id&gt;</code>. Most logging and many client libraries print only <code>error.message</code> and discard <code>error.data</code>, which is why the answer stays hidden.'),
        ('The docs say custom client fields have no API. Are they wrong?', 'On this point, yes. The documented field list for addClient omits custom fields entirely, yet supplying a <code>client_fields</code> map keyed by the ids from the error payload works. We verified it against a live account.'),
        ('Can I hard-code the field ids?', 'Don&rsquo;t. They&rsquo;re per-account 32-character identifiers, so an integration that hard-codes them breaks on the next account. Discover them at runtime from the error and fill what you&rsquo;re told to fill.'),
        ('Is unticking &ldquo;required&rdquo; in the admin an alternative?', 'It is, if you can find the setting and don&rsquo;t need the field enforced elsewhere. On newer accounts the screen is called &ldquo;Intake form for clients&rdquo; rather than &ldquo;Client Fields&rdquo;, which is one reason people struggle to locate it. Handling it in code is more robust because it survives someone re-ticking the box.'),
    ],
    cta_args=('Thinking about your own booking experience?', 'We built ours on SimplyBook and it is live on this site &mdash; have a look, then tell us what you are trying to do. Quoted per project, honestly, after a proper look.', ('See our live booking page', '/book-service/'), ('Talk it over', '/contact/')),
)

info_page(
    slug='simplybook-callback-url-not-working',
    crumb_name='Callback URL not working',
    title='SimplyBook Callback URL Not Working? The Setup Nobody Explains',
    og_title='SimplyBook callback URL &mdash; the setup nobody explains',
    eyebrow='// SIMPLYBOOK API',
    h1='SimplyBook <em class="grad grad--cyan">callback URL</em>: the setup nobody tells you about',
    lede='It is empty by default, it is unsigned, it warns you about nothing, and hosting bot-protection can eat it silently. What SimplyBook actually posts, how to secure it, and how to make its silence visible.',
    desc='SimplyBook.me callback URL not working? What it actually POSTs (booking_id, booking_hash, notification_type), how to authenticate an unsigned webhook, why returning 200 on failure loses bookings, and why "configured" does not mean "working".',
    chips=['Unsigned by default', 'Silent failures', 'From a live build'],
    hero_cta1=('Custom SimplyBook integration', '/simplybook-integration/'),
    hero_cta2=('Talk to us', '/contact/'),
    pre=None,
    inner='<h2>Where the callback URL actually lives</h2>\n\n<p>In SimplyBook&rsquo;s admin, go to <strong>Settings &rarr; API</strong>. On that page, alongside your API key, there is a single box labelled <strong>Callback URL</strong>. That one field is the whole webhook configuration. There is no event picker, no secret field, no signing key, no delivery log to inspect afterwards. You paste a URL, save, and SimplyBook will POST to it when bookings change.</p>\n\n<p>By default that box is <strong>empty</strong>. This is the single most expensive fact in this guide, so it is worth stating plainly: if you never fill it in, nothing reaches your server, and nothing anywhere tells you so. Your integration will happily create bookings through the API and look perfectly healthy, while every booking taken inside SimplyBook itself &mdash; by a member of staff, by a customer using a SimplyBook page, by an admin moving an appointment &mdash; is invisible to you. There is no warning banner, no red dot, no &ldquo;you have no callback configured&rdquo; nudge. Silence looks exactly like success.</p>\n\n<h2>What SimplyBook actually POSTs</h2>\n\n<p>The request is a raw JSON body (not form-encoded), and it is deliberately thin. You get:</p>\n\n<ul>\n<li><code>booking_id</code> &mdash; the booking&rsquo;s numeric id</li>\n<li><code>booking_hash</code> &mdash; a per-booking hash you need in order to read it back</li>\n<li><code>company</code> &mdash; your company login</li>\n<li><code>notification_type</code> &mdash; one of <code>create</code>, <code>cancel</code>, <code>notify</code> or <code>change</code></li>\n</ul>\n\n<p>That is it. No customer name, no service, no start time, no price. The callback is a doorbell, not a delivery: it tells you something happened to a booking and leaves you to go and fetch the detail yourself. Design for that from the start &mdash; treat the payload as an identifier plus a hint, and never try to render a confirmation from the webhook body alone.</p>\n\n<p>Because the body is JSON, read the raw input stream rather than the usual form-parsed superglobals. Plenty of half-finished integrations fail here first: the endpoint returns 200, the framework sees an empty request, and the booking quietly evaporates.</p>\n\n<h2>It is unsigned &mdash; you must authenticate it yourself</h2>\n\n<p>There is no HMAC header, no shared signing secret, no signature to verify. Anyone who learns your callback URL can POST whatever they like to it. Since the URL is the only thing SimplyBook lets you configure, the URL has to carry the secret:</p>\n\n<pre><code>https://example.co.uk/api/booking-callback.php?t=&lt;long-random-token&gt;</code></pre>\n\n<p>Compare that token in constant time &mdash; <code>hash_equals()</code> in PHP &mdash; rather than with <code>==</code>, so the comparison cannot be probed a character at a time. Then check the <code>company</code> field in the payload against your own company login and reject anything that does not match. Both checks are cheap, and together they mean a stranger needs your exact token and your company name before they can even make your code do work.</p>\n\n<p>A few habits that go with an unsigned webhook: generate the token from a cryptographically secure source and keep it out of your public repository, keep it out of client-side code entirely, and rotate it by pasting a new URL into the Settings &rarr; API box if you ever suspect it has leaked. Log rejected requests, but log the fact of the rejection, not the token that was offered.</p>\n\n<h2>Reading the booking back</h2>\n\n<p>Once the request is authenticated, fetch the real detail with the admin API method <code>getBookingDetails(id, sign)</code>. The signature is a plain MD5 of three values concatenated in order &mdash; the booking id, the booking hash from the callback, and your API secret key:</p>\n\n<pre><code>sign = md5(bookingId . bookingHash . secretKey)</code></pre>\n\n<p>The hash arrives with the callback precisely so you can prove you received a genuine notification for that booking. Do not cache it, do not put it in a URL you hand to a browser, and do not log it next to the booking id.</p>\n\n<p>One rule that saves a lot of grief: <strong>trust the booking&rsquo;s own status, not the order the webhooks arrived in.</strong> Duplicate and late deliveries happen. You will see a <code>create</code> land after a <code>cancel</code> for the same appointment, or the same <code>create</code> twice. If you build state from the event stream you will resurrect cancelled appointments and email customers about slots that no longer exist. Build state from the booking record you just fetched &mdash; if it reports itself cancelled, it is cancelled, whatever the notification said. Make the handler idempotent, keyed on the booking id, so a repeat delivery is a no-op rather than a second confirmation email.</p>\n\n<h2>Return the right status code, or lose the booking</h2>\n\n<p>SimplyBook retries on a non-2xx response. That is your only safety net, and it is easy to throw away.</p>\n\n<p>The failure mode looks like this: your handler receives the callback, tries <code>getBookingDetails</code>, the API call times out or the token refresh fails, and your code &mdash; being polite &mdash; catches the error and returns 200 so the caller is not left hanging. SimplyBook now believes the notification was delivered successfully and never sends it again. That booking is gone from your system permanently, and the customer&rsquo;s confirmation email is never sent.</p>\n\n<p>So: return <strong>5xx on any transient failure</strong> &mdash; the lookup failed, the queue file could not be written, the database was unreachable. Return 200 only when you have genuinely finished with the message, which usually means &ldquo;safely persisted, will process shortly&rdquo;. Accept-then-process is the right shape here: write the job somewhere durable, respond 200, do the slow work afterwards. Reserve a 200 for authentication failures too, oddly enough &mdash; there is no point asking SimplyBook to retry a request you will always reject.</p>\n\n<h2>Configured is not the same as working</h2>\n\n<p>Filling the box in does not prove the POST arrives. Shared hosting bot-protection sits in front of your site and does not know the difference between a scraper and a legitimate server-to-server POST from an API vendor. We have seen a hosting platform&rsquo;s protection intercept exactly this kind of request, so the URL was correct, the code was correct, and nothing ever ran. Nothing in SimplyBook&rsquo;s interface reports a failed delivery, so from the admin side it looks fine.</p>\n\n<p>The fix is not more logging &mdash; nobody reads logs that are usually empty. Make the callback <strong>announce itself</strong>. Have the handler post a short line to a chat channel you already watch (we relay ours into Slack) for every booking it receives: type, service, customer, start time. Once every real booking produces a visible message, silence becomes the alarm. If the diary has a booking in it and no message appeared, you know within minutes that the pipe is broken, rather than finding out when a customer arrives for an appointment you never saw.</p>\n\n<p>The same announcement doubles as your test harness. Make a booking in the admin UI, watch for the message. That is the only proof that matters.</p>\n\n<h2>There is no reminder webhook</h2>\n\n<p>Worth knowing before you design your notification stack: SimplyBook fires <strong>no callback for reminders</strong>. The four notification types are all about the booking record changing. Reminders are purely time-based inside SimplyBook &mdash; they go out on its schedule, through its templates, and your server hears nothing.</p>\n\n<p>If you want your own reminder emails, with your own wording and your own branding, you have to schedule them yourself from the booking start time when the <code>create</code> callback lands. That means storing the start time, computing your own send times, and re-checking the booking&rsquo;s current status before each send so you do not remind someone about an appointment they cancelled last week. It is not difficult, but it is entirely on you, and it is not obvious from the documentation.</p>\n\n<p>We built all of this for our own booking system at 365 Techies in July 2026 &mdash; the proxy, the authenticated callback, the queue, the reminders &mdash; because we wanted our customers to deal with us rather than with someone else&rsquo;s booking widget. If you are wiring SimplyBook into your own site and would rather not rediscover these traps one lost booking at a time, we are happy to help.</p>',
    faqs=[
        ('Where is the callback URL set?', 'In SimplyBook admin under <strong>Settings &rarr; API</strong>, in a single &ldquo;Callback URL&rdquo; box. It is empty by default, and nothing anywhere warns you that your integration is therefore deaf to every booking made inside SimplyBook.'),
        ('Is the callback signed?', 'No. The POST carries <code>booking_id</code>, <code>booking_hash</code>, <code>company</code> and <code>notification_type</code>, with no signature. You must authenticate it yourself &mdash; a secret token in the query string compared in constant time, plus a check that the company matches yours.'),
        ('Does SimplyBook send a webhook for reminders?', 'No. Reminders are purely time-based, so there is no event to hook. If you want your own reminder emails you must schedule them yourself from the booking start time.'),
        ('It looks configured but nothing arrives. What now?', 'Check whether your host&rsquo;s bot-protection is intercepting server-to-server POSTs before they reach your script &mdash; that is a common and completely silent cause. The durable fix is to make every received callback announce itself somewhere you look, so that silence becomes the alarm.'),
    ],
    cta_args=('Thinking about your own booking experience?', 'We built ours on SimplyBook and it is live on this site &mdash; have a look, then tell us what you are trying to do. Quoted per project, honestly, after a proper look.', ('See our live booking page', '/book-service/'), ('Talk it over', '/contact/')),
)

info_page(
    slug='simplybook-custom-booking-page',
    crumb_name='Custom booking page',
    title='Replacing the SimplyBook Widget With Your Own Booking Page',
    og_title='Replace the SimplyBook widget with your own booking page',
    eyebrow='// SIMPLYBOOK API',
    h1='Replacing the SimplyBook widget with <em class="grad grad--cyan">your own booking page</em>',
    lede='The architecture that keeps your API keys safe, the calls that power the flow, and the details that decide whether people finish booking or give up.',
    desc='How to replace the SimplyBook.me widget with your own booking page: the server-side proxy pattern that keeps credentials private, which API calls power services and availability, and the UX details that decide whether people finish.',
    chips=['Credentials stay server-side', 'No iframe', 'Live example'],
    hero_cta1=('See ours in action', '/book-service/'),
    hero_cta2=('Custom integration', '/simplybook-integration/'),
    pre=None,
    inner='<h2>Why replace the widget at all</h2>\n\n<p>SimplyBook&rsquo;s embeddable widget is a reasonable starting point &mdash; it works, and it costs nothing to paste in. But once a booking page becomes a real part of how you win work, an iframe starts to cost you in ways that are hard to see on a screenshot.</p>\n\n<ul>\n<li><strong>Branding.</strong> The widget looks like the widget. Your fonts, spacing, tone of voice and buttons stop at the iframe boundary, right at the moment a customer is deciding whether to trust you.</li>\n<li><strong>Page speed.</strong> You are loading a third-party bundle, its styles and its own network round-trips inside your page. You have no control over what it pulls in or when.</li>\n<li><strong>Cookies and consent.</strong> Third-party frames drag third-party storage into your consent story. If you have worked to keep your cookie banner honest and minimal, an embedded booking tool undoes some of that.</li>\n<li><strong>Accessibility.</strong> You are responsible for how your site behaves with a keyboard and a screen reader, but you cannot fix markup that lives inside someone else&rsquo;s frame.</li>\n<li><strong>Conversion control.</strong> You cannot re-order steps, change wording, pre-select a service from the page the visitor arrived on, or run your own analytics on the drop-off points. An iframe is also at the mercy of the provider&rsquo;s frame policy &mdash; if it declines to render, your booking page is simply blank.</li>\n</ul>\n\n<p>Replacing it does not mean leaving SimplyBook. In our own setup, SimplyBook.me is the diary <em>engine</em> only. Customers never see it, and never know it is there. It keeps the calendar, the staff, the working hours and the notifications; the booking journey is ours.</p>\n\n<h2>The architecture that keeps it safe</h2>\n\n<p>The single most important rule: <strong>the browser never sees an API key</strong>. Everything goes through one small server-side proxy &mdash; in our case a PHP file, <code>api/pcm-booking.php</code>, on ordinary shared hosting. Credentials live in a server-only file outside the public repo. The front end talks only to the proxy, and the proxy decides what is allowed.</p>\n\n<pre><code>browser  &rarr;  /api/pcm-booking.php?action=...   (rate-limited per IP, cached ~90s)\n         &rarr;  https://user-api.simplybook.me/     (tokens cached ~20 min server-side)\n</code></pre>\n\n<p>Read-only actions &mdash; the service list and availability &mdash; are public, because a booking page has to work before anyone identifies themselves. Anything that <em>writes</em> requires proof of identity first. We use our own passwordless emailed-code sign-in for that, because SimplyBook client accounts are self-registerable with any email address: a SimplyBook client login proves someone controls that booking account, nothing more.</p>\n\n<h2>Which calls power the flow</h2>\n\n<ol>\n<li><strong>Token.</strong> The public API token comes from <code>getToken(company, apiKey)</code> posted as JSON-RPC to <code>https://user-api.simplybook.me/login</code>. The admin token comes from <code>getUserToken(company, user, userKey)</code>; admin calls then go to <code>https://user-api.simplybook.me/admin/</code> with <code>X-Company-Login</code> and <code>X-User-Token</code> headers. Cache both server-side (about twenty minutes) so you are not fetching a token per page view.</li>\n<li><strong>Service list.</strong> Pull the services &mdash; names, durations and descriptions &mdash; from SimplyBook rather than hard-coding them in your HTML. This matters more than it sounds; see below.</li>\n<li><strong>Availability.</strong> <code>getStartTimeMatrix</code> returns the bookable start times for a service over a date range. This is what you render as your slot picker.</li>\n<li><strong>Unit (staff) selection.</strong> <code>getAvailableUnits</code> tells you who can actually take the chosen service at the chosen time.</li>\n<li><strong>The booking itself</strong> &mdash; made with the admin <code>book</code> call, against a real client id.</li>\n</ol>\n\n<h3>Why the admin book call, with a real client id</h3>\n\n<p>If your account requires client authorisation, a booking attempted through the public path comes back with a client-authorisation error, because there is no logged-in SimplyBook client behind the request. Booking as admin with a client id you already hold sidesteps that entirely: you find or create the client server-side, then book on their behalf.</p>\n\n<p>Creating that client is where most integrations stall. <code>addClient</code> can fail permanently with &ldquo;Value is required and can&rsquo;t be empty&rdquo; even when every documented field is present. The live JSON-RPC error is code <code>-32070</code>, and its payload carries <code>data.field</code> naming the exact missing field as <code>client_fields/&lt;32-character hex id&gt;</code> &mdash; a required custom client field. Supplying <code>client_fields: { "&lt;32-hex id&gt;": "value" }</code> inside the client-data object makes the create succeed. Note that <code>getCompanyParam(\'require_fields\')</code> only reports required <em>built-in</em> fields; it says nothing about custom ones. Many client libraries truncate <code>error.data</code>, which is why this looks unsolvable until you log the raw response.</p>\n\n<h2>Rate limiting and short caching, because it is not your API</h2>\n\n<p>Your public endpoints are a thin skin over a third party you do not control. Anyone can hit them, in a loop, from anywhere. We rate-limit per IP and cache the read-only responses for about ninety seconds &mdash; long enough that a burst of traffic or a bot costs one upstream call, short enough that a slot booked a minute ago disappears quickly. Cache the token separately and for longer; cache availability briefly and per service/date key.</p>\n\n<h2>The UX decisions that mattered</h2>\n\n<ul>\n<li><strong>Plain-English times.</strong> Show &ldquo;2:30pm&rdquo;, not &ldquo;14:30&rdquo;. Keep the 24-hour value in the data attribute; put the human one on the button.</li>\n<li><strong>Descriptions from SimplyBook.</strong> Pull service names and descriptions from the API so the owner can edit them in one place without touching the site. It also stops the page and the diary drifting apart.</li>\n<li><strong>The slot-taken race.</strong> Availability you fetched ninety seconds ago may be gone. Assume the booking can fail on submit: catch it, re-fetch the matrix, show the nearest alternatives with an honest message, and keep the customer&rsquo;s details filled in.</li>\n<li><strong>Never fail silently.</strong> A booking page that quietly does nothing is worse than one that says &ldquo;something went wrong &mdash; please ring us on 01202 775566&rdquo;. Log every failure server-side and make failures announce themselves somewhere a human looks. Silence is the one failure mode you will not notice.</li>\n<li><strong>Watch for recurring services.</strong> A service configured as recurring returns the whole series from a single <code>book()</code> call &mdash; the response&rsquo;s <code>bookings</code> array contains every occurrence. Read the whole array. We booked one appointment and quietly created twelve, running out to November 2027, because we only looked at <code>bookings[0]</code>.</li>\n</ul>\n\n<h3>Accessibility, briefly</h3>\n\n<p>Slots and services should be real <code>&lt;button&gt;</code> elements, reachable and operable by keyboard, with <code>aria-pressed</code> on the selected one. Resist the temptation to build fake tabs out of <code>&lt;div&gt;</code>s. Announce step changes, keep focus somewhere sensible after each step, and make sure error text is associated with the field it belongs to.</p>\n\n<p>We built this for our own booking system in 2026, because the widget was not good enough for how we wanted our customers treated. Everything above is what it cost us to learn. If you are looking at the same problem, we are happy to talk it through &mdash; or to build it.</p>',
    faqs=[
        ('Can I call the SimplyBook API straight from the browser?', 'No &mdash; not safely. Anything in the browser is readable by anyone, so your API key would be public. Put a small server-side proxy in between: the browser talks only to your own domain, and the credentials never leave your server.'),
        ('Why book through the admin method rather than the public one?', 'On accounts configured to require client authorisation, unauthenticated public booking is rejected outright. Resolving or creating the client first and booking with a real client id sidesteps that entirely.'),
        ('Do I need to rate-limit my own booking page?', 'Yes, if the endpoints are public. Service lists and availability hit a third-party API you don&rsquo;t control, so a scraper hammering them can burn the quota your real bookings depend on. Rate-limit per IP and cache briefly.'),
        ('What breaks conversions most?', 'Silent failures and jargon. Show times as &ldquo;2:30pm&rdquo; rather than &ldquo;14:30&rdquo;, handle the slot-just-taken race gracefully, and never let an error end in a dead screen with no phone number on it.'),
    ],
    cta_args=('Thinking about your own booking experience?', 'We built ours on SimplyBook and it is live on this site &mdash; have a look, then tell us what you are trying to do. Quoted per project, honestly, after a proper look.', ('See our live booking page', '/book-service/'), ('Talk it over', '/contact/')),
)

info_page(
    slug='simplybook-custom-email-notifications',
    crumb_name='Custom email notifications',
    title='Taking Over SimplyBook&rsquo;s Customer Emails | 365 Techies',
    og_title='Taking over SimplyBook&rsquo;s customer emails',
    eyebrow='// SIMPLYBOOK EMAILS',
    h1='Taking over SimplyBook&rsquo;s <em class="grad grad--cyan">customer emails</em>',
    lede='Their templates can only be edited in their admin, and the feedback email sends your customers to their review page rather than your Google profile. Here is how to replace the lot safely &mdash; and the switch-off order that avoids missed appointments.',
    desc='How to replace SimplyBook.me customer emails with your own: why templates are admin-only, the safe parallel-run switch-off order, the envelope-sender bug that sends mail to spam, and the UK law on rewarding reviews versus referrals.',
    chips=['Own your review ask', 'Parallel-run first', 'Deliverability detail'],
    hero_cta1=('Custom SimplyBook integration', '/simplybook-integration/'),
    hero_cta2=('Talk to us', '/contact/'),
    pre=None,
    inner="<h2>What SimplyBook actually sends your customers</h2>\n<p>Out of the box SimplyBook.me sends five kinds of email to the client: a booking confirmation, a reminder before the appointment, a change notice if the booking is edited, a cancellation notice, and a feedback request some days afterwards. Provider (staff) notifications are a separate set with their own switches &mdash; turning off a client email does not turn off the matching one to your team, and vice versa. Worth knowing before you start hunting for why an email you thought you had killed is still arriving.</p>\n<p>One more thing to check before you assume you know what is being sent: if you have the Multiple booking or Group booking features enabled, those features override the default notification templates. The email your customer receives may not be the template you have been carefully editing.</p>\n\n<h2>Templates are editable only in the admin UI</h2>\n<p>This is the constraint that shapes everything else. The notification templates live in SimplyBook&rsquo;s admin interface and can be edited only there. There is no API surface for them &mdash; you cannot version them in git, you cannot vary them per service from your own code, you cannot A/B them, and you cannot deploy them alongside a release. If you want your booking emails to be part of your codebase rather than part of somebody&rsquo;s SaaS settings screen, the only route is to stop SimplyBook sending them and send your own.</p>\n\n<h2>The feedback request is usually what starts this</h2>\n<p>For most firms the trigger is the automatic feedback request. It points customers at SimplyBook&rsquo;s own review page. That is a perfectly reasonable thing for SimplyBook to do and a slightly odd thing for your business to do, because the review you actually want sits on your Google Business Profile, where it does some good. So the first change most people make is to disable it: Settings &rarr; Email and SMS settings &rarr; &ldquo;Send feedback request X days after booking&rdquo; &rarr; Disable. Then send your own follow-up pointing wherever you want.</p>\n<p>While you are there, one legal point that is easy to get wrong in the UK. <strong>Asking</strong> a customer for an honest review is fine. <strong>Rewarding</strong> them for leaving one &mdash; a discount, a prize draw entry, a free hour &mdash; is not: incentivised reviews fall foul of the Digital Markets, Competition and Consumers Act. Rewarding a <strong>referral</strong> is a different thing entirely and remains fine. If you are writing a follow-up email template, keep those two ideas in separate sentences, because they read very similarly and only one of them is legal.</p>\n\n<h2>Replacing them properly starts at the webhook</h2>\n<p>Your own emails have to be triggered by something, and the only reliable trigger is SimplyBook&rsquo;s callback. Set the Callback URL in the admin under Settings &rarr; API; SimplyBook then POSTs raw JSON containing <code>booking_id</code>, <code>booking_hash</code>, <code>company</code> and <code>notification_type</code>, where the type is one of <code>create</code>, <code>cancel</code>, <code>notify</code> or <code>change</code>. The POST is not signed, so protect the endpoint yourself &mdash; we pass a secret token in the query string and compare it with <code>hash_equals()</code>. Booking detail is then read back with <code>getBookingDetails(id, sign)</code>, where <code>sign = md5(bookingId . bookingHash . secretKey)</code>.</p>\n<p>Do not send the email inside the webhook request. Write it into a queue and return. Our queue is a JSON file holding one state machine per email &mdash; queued, sending, sent, failed &mdash; with file locking so two overlapping callbacks cannot send twice, tmp-file-plus-rename writes so a crash mid-write cannot corrupt the queue, a retry cap so a permanently bad address does not loop forever, quiet hours (we send between 9am and 8pm) and a per-customer dedupe window so a customer who reschedules three times in ten minutes gets one email, not three. Every one of those pieces exists because something went wrong without it.</p>\n<p>Two behaviours to build in from the start. Return a non-2xx status on transient failures so SimplyBook retries; if you return 200 after a failed lookup, that booking is gone for good. And make the callback announce itself somewhere visible &mdash; we post to Slack &mdash; because hosting bot-protection can quietly intercept server-to-server POSTs. &ldquo;Configured&rdquo; is not the same as &ldquo;working&rdquo;, and silence looks identical to no bookings.</p>\n<p>One gap you must plan for: <strong>SimplyBook fires no webhook for reminders.</strong> Reminders are purely time-based inside their system. If you want your own reminder emails, you schedule them yourself from the booking start time you got at creation.</p>\n\n<h2>The switch-off order that avoids disasters</h2>\n<p>Run both systems in parallel for a few weeks. Yes, customers get two confirmations; that is a small, explainable annoyance and it is far better than the alternative. Watch your queue against SimplyBook&rsquo;s own sent log until the two agree on every booking, including ones staff take inside the SimplyBook interface.</p>\n<p>Then switch SimplyBook&rsquo;s emails off <strong>one type at a time</strong>, leaving a week between each, starting with the least critical &mdash; feedback request, then change, then cancel, then reminder. <strong>Never turn off their confirmation email first.</strong> The confirmation is the one email a customer notices the absence of, and it is the one that proves the booking exists. Turn it off last, and only once yours has landed correctly for every booking type you handle, including recurring series and bookings created by staff rather than by your own front end.</p>\n\n<h2>Deliverability: the parameter that ate our sign-in codes</h2>\n<p>This cost us more time than the rest of the project. PHP&rsquo;s <code>mail()</code> does not set an envelope sender unless you tell it to. Without the <code>-f</code> parameter, Return-Path defaults to the hosting account&rsquo;s address, SPF is then checked against <em>that</em> domain rather than yours, the alignment fails, and Gmail either junks the message or rejects it outright. The From: header looks perfect the whole time.</p>\n<pre><code>// Without the fifth argument, Return-Path = the hosting account\nmail($to, $subject, $body, $headers, '-fbookings@yourdomain.co.uk');</code></pre>\n<p>We now send through authenticated SMTP with an explicit envelope sender, which removes the ambiguity entirely. But the missing <code>-f</code> is what stopped our passwordless sign-in codes reaching Gmail, and the symptom &mdash; &ldquo;some customers say they never got it&rdquo; &mdash; is maddeningly vague.</p>\n\n<h3>A forwarded test proves nothing</h3>\n<p>When you test, send directly to an external mailbox you control &mdash; a real Gmail, Outlook and Yahoo address. If you send to your work address and forward it to Gmail to check, Gmail authenticates the <em>forwarder</em>, not you. The message sails through, you declare victory, and your customers carry on not receiving anything. Check the received headers on the direct test: you want SPF pass, DKIM pass and DMARC pass, all aligned to your sending domain.</p>\n\n<p>We built all of this for our own booking system at 365 Techies in July 2026, and everything above is what we found while doing it rather than what the documentation says. If you are looking at the same job and would rather not spend a fortnight discovering the <code>-f</code> parameter, we are happy to talk it through or do it for you.</p>",
    faqs=[
        ('Can the API edit SimplyBook&rsquo;s email templates?', 'No. Notification templates are editable only in the SimplyBook admin interface. If you want your own emails, you switch theirs off and send your own &mdash; which also means inheriting queueing, retries and deliverability.'),
        ('How do I stop the SimplyBook feedback request?', 'In the admin under <strong>Settings &rarr; Email and SMS settings</strong>, set &ldquo;Send feedback request X days after booking&rdquo; to Disable. Firms usually want this because it points customers at SimplyBook&rsquo;s own review page rather than their Google profile.'),
        ('Can I offer a reward for leaving a review?', 'No &mdash; incentivised reviews are unlawful in the UK under the Digital Markets, Competition and Consumers Act. Asking every customer for an honest review is fine. Rewarding a <em>referral</em> is also fine; just keep the two messages completely separate.'),
        ('My emails go to spam. Why?', 'Most often the envelope sender. PHP&rsquo;s <code>mail()</code> without the <code>-f</code> parameter lets Return-Path default to the hosting account, so SPF authenticates the wrong domain and Gmail junks the message. Also note that forwarding a test email proves nothing &mdash; the receiver then authenticates the forwarder, not you.'),
    ],
    cta_args=('Thinking about your own booking experience?', 'We built ours on SimplyBook and it is live on this site &mdash; have a look, then tell us what you are trying to do. Quoted per project, honestly, after a proper look.', ('See our live booking page', '/book-service/'), ('Talk it over', '/contact/')),
)

info_page(
    slug='simplybook-integration-traps',
    crumb_name='Six production traps',
    title='Six SimplyBook Traps That Bite You in Production | 365 Techies',
    og_title='Six SimplyBook traps that bite you in production',
    eyebrow='// SIMPLYBOOK API',
    h1='Six SimplyBook traps that <em class="grad grad--cyan">bite you in production</em>',
    lede='One booking that quietly became twelve. Calendar invites an hour out all summer. A webhook that was never connected. Every one of these cost us time on a live system &mdash; symptom, cause and fix.',
    desc='Six SimplyBook.me integration traps found on a live system: recurring services booking a whole series, iCal files with no timezone, empty callback URLs, required custom client fields, category visibility that hides nothing, and client logins that prove nothing.',
    chips=['All found live', 'Symptom &rarr; cause &rarr; fix', 'Nothing theoretical'],
    hero_cta1=('Custom SimplyBook integration', '/simplybook-integration/'),
    hero_cta2=('Talk to us', '/contact/'),
    pre=None,
    inner='<h2>Traps, not bugs</h2>\n\n<p>Everything below behaved exactly as SimplyBook intended. Each one still cost us time, because the behaviour was invisible until it landed in production. This is what we found building our own booking integration in July 2026 &mdash; a PHP proxy in front of SimplyBook&rsquo;s JSON-RPC API, with our own front end. Symptom first, then cause, then what we actually did about it.</p>\n\n<h2>1. One booking creates twelve</h2>\n\n<p><strong>Symptom.</strong> We booked a single test appointment. The diary filled with twelve appointments, out to November 2027. Our confirmation email told the customer about one of them.</p>\n\n<p><strong>Cause.</strong> The service was configured as recurring. When a recurring service is booked, a single <code>book()</code> call returns the <em>whole series</em> &mdash; the response&rsquo;s <code>bookings</code> array contains every occurrence, not just the one the customer clicked. Our code read <code>bookings[0]</code>, logged it, and threw the rest away. Nothing errored. The extra eleven existed only in SimplyBook&rsquo;s diary, invisible to us.</p>\n\n<p><strong>Fix.</strong> Treat the response as a list, always &mdash; even for services you believe are one-off, because whether a service recurs is a setting somebody can change in the admin UI without touching your code. Iterate the full array. Store every occurrence with its own id. Tell the customer plainly on the confirmation screen and in the email: how many appointments, and the date of the last one. Then queue a reminder per occurrence, not one for the booking.</p>\n\n<pre><code>$res = $rpc-&gt;book(...);\nforeach ($res[\'bookings\'] as $b) {\n    store_occurrence($b);   // not just $res[\'bookings\'][0]\n    queue_reminder($b);\n}</code></pre>\n\n<p>If your customer only ever wanted one appointment, this is the difference between finding out at booking time and finding out when they ring up about the eleven reminders.</p>\n\n<h2>2. Calendar invites land an hour out</h2>\n\n<p><strong>Symptom.</strong> The confirmation email looks right. The customer taps the calendar attachment and the appointment saves an hour away from the time on the page.</p>\n\n<p><strong>Cause.</strong> SimplyBook attaches an iCal file to its emails, and there is a separate setting &mdash; &ldquo;Include TZ into iCal&rdquo; &mdash; which is <strong>off by default</strong>. With it off, the <code>.ics</code> carries no timezone information at all. The receiving calendar has to guess, and through British Summer Time it guesses wrong. In winter you may never notice; from late March you get a steady trickle of people arriving an hour early or an hour late.</p>\n\n<p><strong>Fix.</strong> Turn that setting on. If you generate your own <code>.ics</code> &mdash; we do, because we send our own emails &mdash; do it properly: include a real <code>VTIMEZONE</code> block for Europe/London with the last-Sunday-in-March and last-Sunday-in-October DST rules, and write the start as <code>DTSTART;TZID=Europe/London:</code> rather than a bare local time. A floating local time in an <code>.ics</code> is a bug waiting for the clocks to change.</p>\n\n<h2>3. Nothing reaches your server</h2>\n\n<p><strong>Symptom.</strong> The integration works perfectly in testing, because everything you book you book through your own code. Then someone takes a booking inside SimplyBook &mdash; over the phone, or by editing the diary &mdash; and your system never hears about it. No reminder, no record, no email.</p>\n\n<p><strong>Cause.</strong> The Callback URL box, in SimplyBook admin under Settings &rarr; API, is empty by default. Nothing warns you. There is no &ldquo;webhooks not configured&rdquo; banner, no failed-delivery log to check, because there is nothing to deliver to. Silence looks identical to success.</p>\n\n<p><strong>Fix.</strong> Fill the box in, then prove it fires. SimplyBook POSTs raw JSON containing <code>booking_id</code>, <code>booking_hash</code>, <code>company</code> and <code>notification_type</code> (one of <code>create</code> / <code>cancel</code> / <code>notify</code> / <code>change</code>). Three things to get right:</p>\n\n<ul>\n<li><strong>It is not signed.</strong> Protect the endpoint yourself. We put a secret token in the query string and compare it with <code>hash_equals()</code>. Booking detail is then read back with <code>getBookingDetails(id, sign)</code> where <code>sign = md5(bookingId . bookingHash . secretKey)</code>.</li>\n<li><strong>Return a non-2xx on transient failures</strong> so SimplyBook retries. If your lookup fails and you still return 200, that booking is gone for good &mdash; you have told SimplyBook the delivery succeeded.</li>\n<li><strong>Configured does not mean working.</strong> Hosting bot-protection (SiteGround&rsquo;s, in our case) can intercept server-to-server POSTs before your script ever runs. Make the callback announce itself &mdash; ours posts to Slack &mdash; so silence becomes something you notice rather than something you assume.</li>\n</ul>\n\n<p>Related: SimplyBook fires no webhook at all for reminders. They are purely time-based. If you want your own reminder emails, schedule them yourself from the booking start time.</p>\n\n<h2>4. Client creation refused, with no field named</h2>\n\n<p><strong>Symptom.</strong> <code>addClient</code> fails every time with &ldquo;Value is required and can&rsquo;t be empty&rdquo;. You are sending name, email and phone. You add address, city, country. Same error. Nothing tells you what is missing.</p>\n\n<p><strong>Cause.</strong> Two admin settings block API client creation, and neither appears in the documented parameter list. Required custom Client Fields (Custom Features &rarr; Client Fields; on newer accounts the screen is called &ldquo;Intake form for clients&rdquo;, though SimplyBook&rsquo;s docs describe the older UI) and Mandatory registration fields (Settings &rarr; Email and SMS settings). The documentation for the company administration service states that <code>addClient</code> accepts only name/email/phone/address1/address2/city/zip/country_id, and that custom Client Fields have no API surface. That is wrong.</p>\n\n<p><strong>Fix.</strong> Read the raw error. The live JSON-RPC failure is code <code>-32070</code>, and its payload contains <code>data.field</code> set to <code>client_fields/&lt;32-character hex id&gt;</code> &mdash; naming the exact field that is missing. Most client libraries truncate <code>error.data</code>, which is why this looks like a dead end. Log the whole error object. Then pass a map keyed by those ids inside the client-data object:</p>\n\n<pre><code>client_fields: { "&lt;32-hex id&gt;": "value" }</code></pre>\n\n<p>We verified this live: a dry-run <code>addClient</code> succeeded once it auto-filled the one required custom field. Note that <code>getCompanyParam(\'require_fields\')</code> tells you which <em>built-in</em> fields are required (ours returned &ldquo;email&rdquo;) and says nothing about custom ones &mdash; so it will not save you here.</p>\n\n<h2>5. Hiding a category does not hide its services</h2>\n\n<p><strong>Symptom.</strong> You tidy the diary by hiding a whole category &mdash; internal jobs, staff time, a service you are not selling yet. It disappears from the view you are looking at. It is still publicly bookable.</p>\n\n<p><strong>Cause.</strong> Per-service visibility is the only setting that governs whether a service can be booked. Category visibility affects presentation, not access. A service you believe is private stays reachable.</p>\n\n<p><strong>Fix.</strong> Set visibility on every service individually, and never rely on the category as a container. Then verify from outside your own session: list services through the public API with a plain public token, as an anonymous visitor would, and diff that list against what you intend to sell. We expose service listing through our proxy as a public read-only endpoint anyway, so this check is one request. Do it after every batch of admin changes, because it is the kind of thing that quietly regresses.</p>\n\n<h2>6. A client login is not proof of custom</h2>\n\n<p><strong>Symptom.</strong> You use SimplyBook client accounts as the sign-in for something that matters &mdash; a portal, a discount, a paid feature &mdash; and people who have never paid you anything have access.</p>\n\n<p><strong>Cause.</strong> SimplyBook client accounts are self-registerable with any email address. A successful client login proves exactly one thing: that person controls that booking account. It is not evidence of a relationship, a payment, or an entitlement.</p>\n\n<p><strong>Fix.</strong> Never grant paid entitlements from a booking login. Keep identity and entitlement separate: authenticate however you like, then check entitlement against your own records of who is actually paying. We use our own passwordless emailed-code sign-in rather than SimplyBook&rsquo;s, and signing in never grants a paid tier on its own.</p>\n\n<p>One deliverability note, since emailed codes only work if they arrive: PHP&rsquo;s <code>mail()</code> without the <code>-f</code> envelope-sender parameter lets Return-Path default to the hosting account, SPF then authenticates the wrong domain, and Gmail junks or rejects the message. That single missing parameter stopped our sign-in codes reaching Gmail. We send through authenticated SMTP with an explicit envelope sender now.</p>\n\n<h2>The pattern</h2>\n\n<p>Five of these six are silent. No exception, no red banner, no entry in a log you were already reading. The defence is the same each time: read whole responses rather than first elements, read whole error payloads rather than messages, and make the quiet paths announce themselves so that nothing happening looks different from everything working.</p>\n\n<p>We built this for our own booking system at 365 Techies &mdash; the diary runs on SimplyBook, but our customers only ever see our own pages, emails and sign-in. If you are wiring SimplyBook into something of your own and would rather not rediscover the above, we are happy to talk it through.</p>',
    faqs=[
        ('Why did one booking create twelve appointments?', 'The service was configured as recurring. A single book() call then returns the entire series in the response&rsquo;s bookings array. If your code reads only the first entry, eleven further appointments exist in the diary that neither you nor the customer has been told about.'),
        ('Why are calendar invites an hour out?', 'There is a separate &ldquo;Include TZ into iCal&rdquo; setting, off by default, which leaves the attached .ics file with no timezone at all. Through British Summer Time that lands appointments an hour wrong on some devices.'),
        ('I hid a service category but the services still show. Why?', 'Category visibility does not cascade. Per-service visibility is the only thing that counts, so services you believed were private can remain publicly bookable.'),
        ('Is a SimplyBook client login proof someone is my customer?', 'No. Client accounts are self-registerable with any email address, so a login proves only that someone controls that booking account. Never grant paid entitlements based on one.'),
    ],
    cta_args=('Thinking about your own booking experience?', 'We built ours on SimplyBook and it is live on this site &mdash; have a look, then tell us what you are trying to do. Quoted per project, honestly, after a proper look.', ('See our live booking page', '/book-service/'), ('Talk it over', '/contact/')),
)

