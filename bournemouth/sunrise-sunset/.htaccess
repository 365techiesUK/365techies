# The sunrise & sunset page is served by index.php: the built index.html with today's computed sunrise, sunset and
# day length filled in on the server at request time, for crawlers and AI answer engines that don't run JavaScript
# (see api/bm-sun-ssr.php). index.html stays the build output and the fallback.
DirectoryIndex index.php index.html
