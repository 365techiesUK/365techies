# TEMPORARY PROBE - delete after reading. Not authoritative for any crawler.
# /test-robots.txt matches the same .txt FilesMatch as the real robots.txt, so it
# travels the identical Apache path, and the handler intercepts any URI
# containing "robots.txt" (proved: /myrobots.txt and /xrobots.txt are answered
# even though no such file was ever deployed).
#
# Reading the result:
#   comes back with 1   -> the handler honours a supplied value; SiteGround's
#                          documented override works and the real fix is one line
#   comes back with 10  -> it overwrites unconditionally; the override is broken
#                          on this account and this response IS the ticket evidence
#   comes back 30 bytes -> the response is replaced wholesale, not edited
User-agent: *
Allow: /
Crawl-delay: 1

User-agent: Bingbot
Allow: /
